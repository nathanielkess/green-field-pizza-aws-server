const PAYMENT_CURRENCY = 'cad';

const PI_FUTURE_USAGE = {
  off: 'off_session',
  on: 'on_session'
};

const DEFAULT_USERNAME = 'anom';

const PI_PIZZAGROUP = 'pizza-item-3';

const PAYMENT_CARD = ['card'];

const DELIVERY_DRIVER_FEE = 500;

const PAYMENT_STATUS = {
  confirm_required: 'requires_confirmation'
};

module.exports = {
  PAYMENT_CURRENCY,
  PI_FUTURE_USAGE,
  PI_PIZZAGROUP,
  PAYMENT_CARD,
  DEFAULT_USERNAME,
  DELIVERY_DRIVER_FEE,
  PAYMENT_STATUS
};
